# MLB 선발투수 컨디션 조기 예측

![Python](https://img.shields.io/badge/python-3.10+-blue)

경기 초반 15구만으로 그날 남은 경기의 투수 컨디션(헛스윙 유도력)을 예측합니다.
투수가 **"누구인가"가 아니라 "오늘 어떤 상태인가"** 를 맞히는 문제로 정의했습니다.

### 핵심 결과

| | |
|---|---|
| **문제 재정의** | ERA → whiff%로 타겟 변경 — 구속과의 상관 3배(0.064→0.213), Val R² 150배 |
| **최종 모델 (v4)** | 79 feature · Val R² 0.0918 · Test R² 0.0399 · 컨디션 분류 AUC 0.65 |
| **최대 개선** | 무브먼트 feature 편입으로 Test R² +42% (100-seed 검증, p=3e-33) |

### 주요 작업

- 타겟 지표 재정의 및 예측 가능성 검증
- Feature Engineering (무브먼트·회전축·ACWR 등 79개)
- 모델링·튜닝·SHAP 해석, paired t-test 기반 가설 검증
- 영상에서 투구 장면·투수를 선별해 관절 좌표 추출

---

## 프로젝트 개요

본 프로젝트는 **MLB 선발투수의 경기 초반 투구 데이터를 활용하여 그날 남은 경기의 컨디션(헛스윙 유도력)을 예측**하는 것을 목표로 합니다.
경기 초반 15구(X구간)에서 이미 나타나는 신호를 바탕으로, 실점이 나오기 전에 투수의 당일 컨디션을 조기 판단하고, 컨디션에 영향을 주는 주요 투구 인자를 찾아 조기 교체·불펜 준비 등 벤치 의사결정에 활용하고자 합니다.

핵심 철학은 이 투수가 **"누구인가"(통산 성적·이름값)가 아니라 "오늘 어떤 상태인가"(컨디션)** 를 예측하는 것입니다.

## 프로젝트 목표

- 경기 초반 15구만으로 **이후 타석의 whiff%(헛스윙률)를 예측**하는 회귀 모델 구축
- 컨디션에 영향을 미치는 **주요 투구 인자 파악**(SHAP 기반 해석)
- **"오늘 컨디션이 좋은 날인가"를 이진 분류**로 실용화 (조기 교체 판단 지원)
- 세운 가설을 **통계적으로 검증**하고, 효과 없는 접근은 정직하게 기각

## 수행 내용

- Baseball Savant Statcast Data **EDA(탐색적 데이터 분석)**
- **타겟 지표 선정** — 예측 가능성 기반으로 예측 대상(Y) 재정의
- **X구간 실험** — 경기 초반 어디까지 봐야 예측이 되는지 탐색
- **Feature Engineering** — 무브먼트·회전축·부하(ACWR) 등 도메인 feature 설계
- **결측·이상치 처리 전략 비교**
- **Optuna 하이퍼파라미터 최적화**
- **paired t-test 기반 가설 검증** (모든 feature 채택 결정에 적용)
- **영상 생체역학 파이프라인** 구축 및 융합 실험 (MediaPipe 관절 추출)
- **SHAP 기반 모델 해석** 및 투수 유형별 오차 분석
- **컨디션 이진 분류** 모델 구축

## 데이터 개요

| 항목 | 내용 |
|---|---|
| 출처 | Baseball Savant (Statcast + 투구별 영상 클립) |
| 기간 | **2021 ~ 2025 (5시즌)** |
| 대상 | MLB 30팀 **선발투수** (불펜은 투구수 부족으로 X/Y 구간 분리 불가) |
| 규모 | 정형 **23,225경기** / 영상 매칭 **3,783경기** (투구 영상 약 5.7만 개) |
| 단위 | 1 row = 1경기 (`game_pk` × `pitcher`) |

### 예측 대상 (Y)

```
whiff% = swinging_strike 수 / 전체 스윙 수
전체 스윙 = swinging_strike + swinging_strike_blocked + foul + foul_tip + hit_into_play
- 스윙 5개 미만 경기 제외 (분모 안정성)
```

### 데이터 분할

**시즌 기반 split** (랜덤 분할 금지 — leakage 방지):

| 구분 | 시즌 | 용도 |
|---|---|---|
| Train | 2021 ~ 2023 | 학습 |
| Validation | 2024 | 모델 선택 |
| Test | 2025 | 최종 평가 |

## 주요 결과

### 1. 예측 대상(Y) 재정의 — 예측 가능한 문제 찾기

모델을 개선하기 전에 "이게 애초에 예측 가능한 문제인가"를 상관 분석으로 먼저 검증했습니다. 투수의 대표 지표인 **ERA부터 시작**해 4개 지표를 비교했습니다.

| 타겟 (Y) | 최고 상관 (구속과) | Val R² | 판정 |
|---|---|---|---|
| ERA (평균자책, 대표지표) | 0.064 | 0.0006 | 수비·운 노이즈 과다 (DIPS 이론) |
| FIP (수비무관 자책) | 0.101 | 0.0093 | 여전히 결과 기반 |
| xwOBA (기대 가중출루율) | 0.105 | 0.0211 | 타구 질로 운 일부 제거 |
| **whiff% (헛스윙률)** | **0.213** | **0.0934** | **투수가 직접 통제 → 채택** |

ERA→whiff%로 갈수록 상관이 **약 3배(0.064→0.213)**, Val R²가 **150배 이상(0.0006→0.093)** 상승했습니다. 결과(안타·실점)에 의존하는 지표일수록 운·수비가 섞여 예측 불가였고, 투수가 직접 통제하는 헛스윙률만 예측 가능했습니다.

### 2. 최종 모델 — 정형 Statcast 단독

| 지표 | 값 |
|---|---|
| 모델 | XGBoost (Optuna 튜닝) |
| Feature 수 | **79** |
| Val R² | 0.0918 |
| **Test R²** | **0.0399** |
| 컨디션 이진분류 Test AUC | 0.65 |

<img src="outputs/figures/01_pred_vs_actual.png" width="680">

> R²가 낮아 보이지만, 선행 연구에서도 구속으로 ERA를 예측하면 R² ≈ 0.05~0.08, 최고 지표 SIERA조차 다음 시즌 ERA의 20%만 설명합니다. 경기 단위 + 초반 15구라는 훨씬 어려운 조건에서 Val R² 0.09는 **문제의 천장에 근접한 값**입니다. ([docs/참고논문/04_타겟지표](docs/참고논문/04_타겟지표/08_target_metrics.md) 참조)

### 3. 무브먼트 feature — 최대 성능 개선

기존 모델은 구속·회전수·릴리스 위치는 담았으나, **"공이 실제로 얼마나 휘었나(무브먼트, pfx)"** 라는 헛스윙의 직접적 원인이 빠져 있었습니다. 무브먼트·회전축·제구 feature를 단계적으로 편입한 결과:

| 모델 | feature | Test R² | 개선 |
|---|---|---|---|
| 정형 baseline + ACWR (v2) | 62 | 0.0274 | — |
| + 무브먼트·회전축·제구 (v3) | 75 | 0.0390 | +42% (p=3e-33) |
| **+ 무브먼트 gap (v4)** | **79** | **0.0399** | Val +0.0012 (p=0.001) |

100-seed paired t-test로 재현성·회귀·분류를 전부 통과한, 프로젝트 전체 최대 개선입니다.

**무브먼트 gap — 쪼개기 vs 차이**: v3의 무브먼트는 구종 3그룹 *평균*이라, 그룹 안에서 정보가 뭉개집니다. 이를 복원하는 방법으로 "구종을 더 잘게 쪼개기"(직구 3종·8구종)와 "구종 간 차이(gap, 직구−변화구 무브먼트)"를 비교했는데, **쪼개기는 표본 손실로 재현성에 실패하고 gap만 통과**했습니다. gap은 안정적인 두 그룹 평균의 차이라 표본 손실 없이 "구종 간 궤적 대비"라는 새 정보를 더합니다 — 헛스윙은 "직구인 줄 알고 스윙했는데 변화구가 와서" 나오므로, 궤적 차이가 클수록 유인력이 높아집니다. ([outputs/final/movement_gap_results.md](outputs/final/movement_gap_results.md) 참조)

### 4. 컨디션 이진 분류 — 실용화

정확한 whiff% 값 예측은 어렵지만, "오늘 컨디션이 좋은 날인가"로 문제를 바꾸면 실용적인 성능이 나옵니다 (whiff% 상위 33% = 좋은 날 / 하위 33% = 나쁜 날).

| 모델 | Val AUC | Test AUC | '좋은 날' Recall |
|---|---|---|---|
| XGBClassifier (정형) | 0.651 | 0.597 | 74% |

### 5. SHAP — 컨디션의 정체

<img src="outputs/figures/02_shap_beeswarm.png" width="600">

**컨디션 신호 Top 5**: 평균 구속 · 구속 변동성(std) · 회전수 · 평소 대비 기준선(prev) · 릴리스 포인트
→ 컨디션 = 절대 구속·회전수 + 얼마나 일정한가(변동성) + 평소 대비 어떤가

## 영상 생체역학 파이프라인

Statcast는 "공의 결과"(구속·회전수)는 정밀하게 담지만 **투수의 몸이 어떻게 움직였는지**는 담지 않습니다. "폼이 무너졌다"는 감각을 데이터로 잡아낼 수 있는지 검증하기 위해, 투구 영상에서 관절 좌표를 추출하는 파이프라인을 설계했습니다.

```
9초 클립 다운로드
  → 컷 분할 (PySceneDetect) → 투구 장면 필터링 (YOLOv8 사람 검출 + 그라운드 비율)
  → 투수 선택 (전신 키포인트 + 화면상 y좌표 최대)
  → 릴리스 프레임 특정 (팔 뻗음 × 속도 최대)
  → MediaPipe Pose 14개 관절 좌표 추출
  → 좌투 미러링 → 어깨너비 정규화 → 이상치 윈저라이징
  → 각도 9종 계산 (stride, arm_slot, shoulder_tilt, trunk_angle, separation ...)
  → 경기 단위 집계 (mean/std/full9 × 초반 3/5/10/15구)
```

<p float="left">
  <img src="notebooks/2_video/figures/01_release_detection_failure.png" width="370">
  <img src="notebooks/2_video/figures/02_release_detection_success.png" width="370">
</p>

> 좌: 검출 실패 — 움직임이 큰 타자를 투수로 오인. 우: 검출 성공 — 투구 동작 중인 투수를 특정해 릴리스 순간(빨간 점)까지 포착. 오검출 필터링이 파이프라인의 핵심 작업이었습니다.

- **처리량**: 5시즌 × 5슬롯 분산 처리로 투구 영상 약 5.7만 개 → 3,783경기 매칭
- **좌투 미러링**: 좌/우투수를 던지는 팔 기준으로 통일해 동일 기준 비교
- **정규화**: 카메라 각도·체격 차이 제거를 위해 어깨너비 기준 정규화

## 가설 검증 — 정직한 기각

성능을 부풀리는 대신, 세운 가설을 **30~100 seed paired t-test**로 검증하고 효과 없는 것은 기각했습니다.

| 가설 | 검증 결과 | 판정 |
|---|---|---|
| delta(평소 대비 편차) feature **강화** | 차이 ≈ 0, 절반은 음수 | 기각 — 기존 구성은 유지 |
| **영상 생체역학 융합** | ΔR² -0.0092, **p ≈ 9.6×10⁻⁷ (유의 악화)** | 기각 |
| 결측 처리 5종 / 이상치 처리 4종 | baseline이 최선 | 기각 |
| 시퀀스 모델(1D-CNN) / changepoint | 유의한 개선 없음 | 기각 |
| 구종 세분화(직구 3종·8구종) | 재현성 실패 | 기각 |

> 같은 "무브먼트를 더 쓰자"는 아이디어여도, **구종을 쪼개는 방식은 표본 손실로 재현성에 실패**했지만 **구종 간 차이(gap)는 재현성을 통과**해 v4에 채택됐습니다(3장 참조). 접근 방식에 따라 재현성이 갈린 대표 사례입니다.
>
> ℹ️ **"기각"의 의미**: 위 표의 기각은 **"그 방향으로 더 밀고 나가자는 가설"이 기각**됐다는 뜻입니다. 예를 들어 delta는 이미 기본 feature 구성에 15개가 들어 있고 최종 모델(79)에도 그대로 남아 있습니다 — 검증 결과 추가 강화가 무의미했을 뿐, 빼기로 한 것이 아닙니다. 반면 영상 생체역학은 최종 모델에서 **실제로 제외**됐습니다.

<img src="outputs/figures/shap_bio_comparison.png" width="680">

> 좌: SHAP Top 30 (파랑=정형, 주황=영상) — 영상 feature는 11위부터 등장. 우: feature 그룹별 평균 |SHAP| — 정형(0.00070)이 영상(0.00032)의 2배 이상.

**영상 융합 기각 과정의 교훈 — SHAP 중요도 ≠ 예측 기여**: 영상 피처가 SHAP 중위권에 올라도 실제 융합 시 성능이 떨어졌습니다. 1차(절반 데이터)에서 "무의(p>0.05)"였던 결론이 전체 데이터(3.3배)로 재검증하자 "유의하게 악화"로 명확해졌습니다. 표본을 늘려 결론의 신뢰도를 높인 과정 자체가 검증 능력의 근거입니다.

## 모델 신뢰 범위 (오차 분석)

평균 R²만 보지 않고, 어떤 투수에게 신뢰할 수 있는지 유형별로 쪼개 확인했습니다.

<img src="outputs/figures/05_error_by_speed_type.png" width="600">

| 투수 유형 | Test R² | 분류 AUC | 해석 |
|---|---|---|---|
| 파워형 (구속↑) | +0.018 | 0.63 | 모델이 잘 맞는 구간 — 구속·회전수 신호 뚜렷 |
| 핀포인트형 (제구 위주) | -0.024 | 0.58 | 예측 어려움 — 위력이 아닌 제구로 승부 |

→ "모든 투수"가 아니라 **"파워형 투수에게 우선 적용"** 이라는 실용 범위를 정의했습니다.

## 프로젝트 구조

```
├── notebooks/               # 실행 순서 = 폴더·파일 번호
│   ├── 1_statcast/          정형 데이터 파이프라인
│   │   └── 01_data_collection → 02_preprocessing
│   │       → 03_feature_engineering → 04_build_targets
│   ├── 2_video/             영상 생체역학 파이프라인
│   │   └── 01_video_download → 02_video_collect
│   │       → 03_skeleton → 04_video_pipeline
│   ├── 3_modeling/
│   │   ├── 1_pipeline/      최종 모델로 이어지는 본류
│   │   │                    04_baseline · 06_x_interval · 07_delta
│   │   │                    10_tuning · 11_feature_selection · 13_final_evaluation
│   │   ├── 2_experiments/   검증 실험 (결측·이상치·영상·시퀀스·changepoint)
│   │   └── 3_analysis/      SHAP · 타겟 비교 · 컨디션 분류
│   └── 4_eda/               타겟 분포 EDA
│
├── src/                     재사용 모듈
│   ├── statcast/            feature_aggregator.py  (X구간 feature 집계, 핵심)
│   ├── modeling/            x_interval_experiment.py
│   ├── video/               video_features.py      (영상 파이프라인 로직)
│   └── legacy/              nan_strategies.py · outlier_handler.py
│                            (기각된 실험 기록용 — src/legacy/README.md 참조)
│
├── outputs/
│   ├── experiments/         실험 결과 CSV·JSON (채택·기각 근거)
│   ├── final/               최종 모델 산출물
│   ├── models/              학습된 모델 (.pkl, 저장소 미포함)
│   └── figures/             시각화
│
├── data/                    저장소 미포함 — data/README.md 참조
│   ├── raw/                 Baseball Savant 원본 (불변)
│   ├── interim/             중간 산출물
│   └── video_batches/       영상 배치 처리 결과
│
├── docs/참고논문/           선행 연구 정리 (주제별, R² 천장 근거)
└── config.py                경로 설정 (Colab/로컬 자동 감지)
```

노트북 번호는 프로젝트 전체 통산이라 폴더 안에서 건너뜁니다.
자세한 설명은 [notebooks/3_modeling/README.md](notebooks/3_modeling/README.md) 참조.

## 실행 방법

```bash
pip install -r requirements.txt
```

정형 모델링만 재현할 경우 영상 관련 4개 패키지(opencv, mediapipe, ultralytics, scenedetect)는 설치하지 않아도 됩니다.

### 코드 검사

```bash
pip install ruff
ruff check .
```

`ruff.toml`에 컨벤션이 정의되어 있고, push·PR 시 GitHub Actions에서 자동 검사됩니다.

### 노트북 실행 순서

폴더·파일명의 번호가 곧 실행 순서입니다.

| 순서 | 경로 | 내용 |
|---|---|---|
| 1 | `notebooks/1_statcast/01 → 04` | 수집 → 전처리 → feature → 타겟 생성 |
| 2 | `notebooks/2_video/01 → 04` | 영상 다운로드 → 스켈레톤 → 각도 집계 *(선택, Colab GPU 권장)* |
| 3 | `notebooks/3_modeling/1_pipeline/` | baseline → X구간 → 튜닝 → 최종 평가 |
| 4 | `notebooks/3_modeling/3_analysis/` | SHAP 해석 · 컨디션 분류 |

`notebooks/3_modeling/2_experiments/`는 본류가 아닌 **검증 실험**입니다. 각 노트북 첫 셀에 결론과 기각 근거가 정리되어 있습니다.

### 데이터 경로 설정

노트북은 Colab과 로컬을 자동으로 감지합니다. 데이터가 프로젝트 루트의 `data/`가
아닌 다른 곳에 있다면 `.env`로 위치를 지정하세요.

```bash
cp .env.example .env
# .env 를 열어 PITCHER_DATA_ROOT 수정
```

`.env`는 gitignore되므로 팀원끼리 서로 다른 경로를 써도 충돌하지 않습니다.
경로 정의는 [config.py](config.py) 한 곳에 모여 있습니다.

### 데이터

원본 데이터(약 614MB)와 학습된 모델 파일은 용량 문제로 저장소에 포함하지 않았습니다.
Statcast 데이터는 `notebooks/1_statcast/01_data_collection.ipynb`로 재수집할 수 있습니다 (Baseball Savant, 2021~2025).

> 노트북은 `IN_COLAB` 자동 감지로 Colab과 로컬 양쪽에서 동작합니다.
> 영상 파이프라인은 GPU가 필요해 Colab 환경을 권장하고, 정형 모델링(XGBoost)은 로컬에서 실행 가능합니다.

## 기술 스택

| 분류 | 사용 기술 |
|---|---|
| 언어 / 처리 | Python, pandas, NumPy, DuckDB (대규모 투구 데이터 SQL 집계) |
| 모델링 | XGBoost, LightGBM, CatBoost, scikit-learn, Optuna |
| 해석 / 검증 | SHAP, SciPy (paired t-test) |
| 영상 처리 | MediaPipe Pose, YOLOv8, PySceneDetect, OpenCV |
| 수집 | requests, BeautifulSoup (Baseball Savant 크롤링) |
| 환경 | Google Colab (GPU) + VS Code 원격, Google Drive |

## 기대 효과

- 실점 전 **당일 투수 컨디션 조기 판단** → 조기 교체·불펜 준비 타이밍 지원
- 컨디션에 영향을 주는 **주요 투구 인자 파악** (무브먼트·구속 변동성 등)
- 중계·분석용 **투수 상태 인사이트** 제공

## 한계점

- **경기 단위 예측의 구조적 천장**: 야구 경기 단위 성과는 상대 타자·그날 스트라이크존 등 투수가 통제할 수 없는 변동이 커, 초반 15구만으로는 Val R² 0.09 수준이 한계입니다(선행 연구의 구속→ERA R² 0.05~0.08과 유사한 범위). 값 자체의 정밀 예측보다 "좋은 날/나쁜 날" 이진 분류(AUC 0.65)가 더 실용적인 이유입니다.
- **핀포인트형 투수 예측의 한계**: 모델은 구속·무브먼트 신호가 뚜렷한 파워형에는 잘 맞지만, 위력이 아닌 제구로 승부하는 핀포인트형에는 예측력이 떨어집니다(Test R² -0.024). 유형별 분리 모델링이 필요한 지점입니다.
- **영상 생체역학의 한계**: 릴리스 순간의 "정지 1프레임 자세 각도"는 whiff%와 인과가 약해 융합 실험이 통계적으로 기각됐습니다. 자세의 *변화·타이밍*을 담으려면 릴리스 전후 시퀀스(동작 궤적) 접근이 필요하나, 이는 별도 연구 범위입니다.

## 팀

팀 프로젝트입니다. 이 저장소는 개인 저장소이며, 다룬 범위는 상단 [주요 작업](#주요-작업)을 참고하세요.

---

<sub>🧪 <b>진행 중인 실험</b> — 팀 프로젝트 이후 개인적으로 이어서, 위 한계점의 "정지 1프레임" 대신 <b>릴리스 전후 프레임 시퀀스</b>로 자세 변화를 다루는 실험을 진행 중입니다. 결과는 확정 전입니다.<br>
<code>notebooks/2_video/05_video_sequence_pipeline.ipynb</code> → <code>notebooks/3_modeling/2_experiments/17_video_sequence_experiment.ipynb</code> · 모듈 <code>src/video/video_sequence_features.py</code></sub>
